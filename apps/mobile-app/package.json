{
  "name": "mobile-app",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@testing-library/jest-native": "*",
    "@testing-library/react-native": "*",
    "react-native": "*",
    "expo": "*",
    "react-native-svg": "*",
    "react-native-web": "*"
  },
  "scripts": {
    "postinstall": "rm -r node_modules && cd ../../ && pnpm install && npx nx sync-deps mobile-app && npx nx ensure-symlink mobile-app"
  }
}
